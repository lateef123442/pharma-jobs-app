<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Pharma Jobs India - Login</title>
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
  <link rel="stylesheet" href="/style.css">
  <style>
    /* Page-specific styles for login */
    body {
      background: linear-gradient(135deg, #e3f2fd 0%, #b2dfdb 50%, #80cbc4 100%);
      font-family: 'Roboto', sans-serif;
      margin: 0;
      padding: 0;
      overflow-x: hidden;
      position: relative;
    }
    body::before {
      content: '';
      position: fixed;
      top: 0;
      left: 0;
      width: 100%;
      height: 100%;
      background: url('data:image/svg+xml,<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 100 100"><circle cx="20" cy="20" r="2" fill="rgba(0,123,255,0.1)"/><circle cx="80" cy="40" r="1.5" fill="rgba(40,167,69,0.1)"/><circle cx="50" cy="80" r="1" fill="rgba(255,193,7,0.1)"/></svg>') repeat;
      animation: float 20s linear infinite;
      z-index: -1;
    }
    .hero {
      background: url('https://via.placeholder.com/1920x600?text=Pharma+Innovation') no-repeat center/cover;
      height: 40vh;
      min-height: 250px;
      display: flex;
      align-items: center;
      justify-content: center;
      color: white;
      text-align: center;
      position: relative;
      overflow: hidden;
    }
    .hero::before {
      content: '';
      position: absolute;
      top: 0;
      left: 0;
      right: 0;
      bottom: 0;
      background: linear-gradient(45deg, rgba(0,123,255,0.7), rgba(40,167,69,0.7));
      animation: pulseGlow 3s ease-in-out infinite alternate;
    }
    .hero h1 {
      z-index: 1;
      font-size: clamp(2em, 5vw, 3em);
      font-weight: 700;
      text-shadow: 0 0 20px rgba(255,255,255,0.8);
      animation: typing 3s steps(30, end), blink-caret 0.75s step-end infinite;
      white-space: nowrap;
      overflow: hidden;
      border-right: 3px solid white;
    }
    .login-container {
      max-width: 450px;
      margin: -50px auto 0;
      background: rgba(255, 255, 255, 0.95);
      backdrop-filter: blur(15px);
      border-radius: 20px;
      padding: 40px;
      box-shadow: 0 15px 35px rgba(0, 0, 0, 0.3), 0 0 0 1px rgba(255,255,255,0.1);
      animation: slideUpBounce 1s ease-out 0.5s both;
      position: relative;
    }
    .login-container::after {
      content: '';
      position: absolute;
      top: -10px;
      left: -10px;
      right: -10px;
      bottom: -10px;
      background: linear-gradient(45deg, #007bff, #28a745, #ffc107);
      border-radius: 25px;
      z-index: -1;
      animation: rotateGlow 5s linear infinite;
    }
    .login-container h2 {
      text-align: center;
      color: #007bff;
      margin-bottom: 30px;
      font-size: 1.8em;
      animation: fadeInScale 0.8s ease-out 1s both;
    }
    .login-container h2 i {
      animation: bounce 1s ease-in-out infinite;
    }
    .form-group {
      margin-bottom: 25px;
      position: relative;
      animation: slideInLeft 0.6s ease-out 1.2s both;
    }
    .form-group:nth-child(2) { animation-delay: 1.4s; }
    .form-group input {
      width: 100%;
      padding: 15px 20px;
      border: 2px solid #ddd;
      border-radius: 12px;
      font-size: 16px;
      transition: all 0.4s cubic-bezier(0.25, 0.46, 0.45, 0.94);
      background: rgba(255,255,255,0.8);
    }
    .form-group input:focus {
      border-color: #007bff;
      box-shadow: 0 0 20px rgba(0, 123, 255, 0.5), inset 0 0 10px rgba(0,123,255,0.1);
      outline: none;
      transform: scale(1.02);
    }
    .form-group i {
      position: absolute;
      right: 20px;
      top: 50%;
      transform: translateY(-50%);
      color: #007bff;
      transition: transform 0.3s;
    }
    .form-group input:focus + i {
      transform: translateY(-50%) scale(1.2);
      color: #28a745;
    }
    .btn-login {
      width: 100%;
      padding: 15px;
      background: linear-gradient(45deg, #007bff, #28a745);
      color: white;
      border: none;
      border-radius: 12px;
      font-size: 18px;
      font-weight: 600;
      cursor: pointer;
      transition: all 0.4s;
      position: relative;
      overflow: hidden;
      animation: slideInUp 0.6s ease-out 1.6s both;
    }
    .btn-login::before {
      content: '';
      position: absolute;
      top: 0;
      left: -100%;
      width: 100%;
      height: 100%;
      background: linear-gradient(90deg, transparent, rgba(255,255,255,0.3), transparent);
      transition: left 0.5s;
    }
    .btn-login:hover::before {
      left: 100%;
    }
    .btn-login:hover {
      transform: translateY(-3px);
      box-shadow: 0 10px 25px rgba(0, 123, 255, 0.6);
    }
    .btn-login:active {
      transform: translateY(0);
    }
    .register-link {
      text-align: center;
      margin-top: 25px;
      animation: fadeIn 0.8s ease-out 2s both;
    }
    .register-link a {
      color: #007bff;
      text-decoration: none;
      font-weight: bold;
      transition: all 0.3s;
      position: relative;
    }
    .register-link a::after {
      content: '';
      position: absolute;
      bottom: -2px;
      left: 0;
      width: 0;
      height: 2px;
      background: #007bff;
      transition: width 0.3s;
    }
    .register-link a:hover::after {
      width: 100%;
    }
    .register-link a:hover {
      color: #0056b3;
      text-shadow: 0 0 10px rgba(0,123,255,0.5);
    }
    /* Animations */
    @keyframes float {
      0% { transform: translateY(0); }
      100% { transform: translateY(-100vh); }
    }
    @keyframes pulseGlow {
      0% { opacity: 0.7; transform: scale(1); }
      100% { opacity: 1; transform: scale(1.05); }
    }
    @keyframes typing {
      from { width: 0; }
      to { width: 100%; }
    }
    @keyframes blink-caret {
      from, to { border-color: transparent; }
      50% { border-color: white; }
    }
    @keyframes slideUpBounce {
      0% { opacity: 0; transform: translateY(50px) scale(0.9); }
      60% { transform: translateY(-10px) scale(1.05); }
      100% { opacity: 1; transform: translateY(0) scale(1); }
    }
    @keyframes fadeInScale {
      0% { opacity: 0; transform: scale(0.8); }
      100% { opacity: 1; transform: scale(1); }
    }
    @keyframes bounce {
      0%, 20%, 50%, 80%, 100% { transform: translateY(0); }
      40% { transform: translateY(-10px); }
      60% { transform: translateY(-5px); }
    }
    @keyframes slideInLeft {
      0% { opacity: 0; transform: translateX(-50px); }
      100% { opacity: 1; transform: translateX(0); }
    }
    @keyframes slideInUp {
      0% { opacity: 0; transform: translateY(30px); }
      100% { opacity: 1; transform: translateY(0); }
    }
    @keyframes fadeIn {
      0% { opacity: 0; }
      100% { opacity: 1; }
    }
    @keyframes rotateGlow {
      0% { transform: rotate(0deg); }
      100% { transform: rotate(360deg); }
    }
    /* Responsiveness */
    @media (max-width: 768px) {
      .hero { height: 30vh; }
      .hero h1 { font-size: 2em; }
      .login-container { margin-top: -30px; padding: 25px; max-width: 90%; }
      .login-container h2 { font-size: 1.5em; }
      .form-group input { padding: 12px 15px; font-size: 14px; }
      .btn-login { font-size: 16px; padding: 12px; }
    }
    @media (max-width: 480px) {
      .hero h1 { font-size: 1.5em; }
      .login-container { padding: 20px; }
      .form-group { margin-bottom: 20px; }
    }
  </style>
</head>
<body>
  <nav class="navbar">
    <div class="nav-brand"><i class="fas fa-pills"></i> Pharma Desk</div>
    <div class="nav-links">
      <a href="/auth/login"><i class="fas fa-sign-in-alt"></i> Login</a>
      <a href="/auth/register"><i class="fas fa-user-plus"></i> Register</a>
    </div>
  </nav>
  <div class="hero">
    <h1>Welcome to Pharma Desk</h1>
  </div>
  <div class="container">
    <div class="login-container">
      <h2><i class="fas fa-sign-in-alt"></i> Login to Your Account</h2>
      <form action="/auth/login" method="POST">
        <div class="form-group">
          <input type="email" name="email" placeholder="Email Address" required>
          <i class="fas fa-envelope"></i>
        </div>
        <div class="form-group">
          <input type="password" name="password" placeholder="Password" required>
          <i class="fas fa-lock"></i>
        </div>
        <button type="submit" class="btn-login">Login</button>
      </form>
      <div class="register-link">
        <p>Don't have an account? <a href="/auth/register">Register here</a></p>
      </div>
    </div>
  </div>
</body>
</html>
